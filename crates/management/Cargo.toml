[package]
name = "management"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
engine = { path = "../engine" }
domain = { path = "../domain" }
rand = "0.10"
serde_json = "1"
chrono = { version = "0.4.45", default-features = false, features = ["std", "serde"] }
